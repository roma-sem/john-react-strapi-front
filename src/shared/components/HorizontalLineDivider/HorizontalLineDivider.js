import React from 'react';
import './HorizontalLineDivider.scss';

export default function HorizontalLineDivider(props) {
    return(
        <h1 className="HorizontalLineDivider">{props.content}</h1>
    );
}
